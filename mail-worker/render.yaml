services:
  - type: web
    name: event-hub-mail-worker
    runtime: docker
    rootDir: mail-worker
    plan: free
    region: frankfurt
    healthCheckPath: /health
    envVars:
      - key: MAIL_WORKER_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
